# 兼容性扩展改进总结

## 概述

为了满足用户对公式和单位提取更灵活、更兼容的需求，我们对以下核心模块进行了扩展：
- `backend/services/data_cleaner.py` - 公式标准化和单位识别
- `backend/services/excel_exporter.py` - 单位提取逻辑

## 改进详情

### 1. 转换公式标准化 - 支持带偏移量的格式

**`FormulaStandardizer.standardize()` 扩展**

#### 原有功能（保持兼容）
- `乘以10` → `10X+0`
- `除以100` → `0.01X+0`
- `×10` / `÷100` 等符号形式
- `量化单位0.5` / `分辨率0.1`

#### 新增功能
- **带偏移量的乘法**：`乘以10+3` → `10X+3` 或 `乘以10-3` → `10X-3`
- **带偏移量的除法**：`除以100+5` → `0.01X+5` 或 `除以100-2` → `0.01X-2`
- **带偏移量的量化/分辨率**：`量化单位0.5+1` → `0.5X+1`
- **符号形式**：`×10+3` / `÷100-2` 同样支持

#### 实现方式
1. 在原有的单一操作数匹配前，先尝试匹配 `操作符 数字1 ± 数字2` 的格式
2. 如果匹配成功，提取两个数字并进行相应运算
3. 如果不匹配，回退到原有的单操作数处理逻辑
4. 使用优化的 `_make_formula` 方法处理负偏移量（生成 `aX-b` 而不是 `aX+-b`）

### 2. 公式检测逻辑 - 支持识别更多变体

**`DataProcessor.process_row()` 中的 `_has_formula_content()` 函数扩展**

#### 改进内容
- 正则表达式更新，支持识别带偏移量的公式描述
- 模式：`乘以[\d.]+\s*[+\-]?\s*[\d.]*` 等
- 保持原有的误识别防护（如排除 `212×21` 这种纯数字乘法）

#### 结果
- 能够准确识别 `乘以10+3`、`除以100-2` 等新格式为公式
- 继续排除 `（数据/212）×21` 这类描述性内容

### 3. 单位提取 - 支持更多识别模式

**`_extract_unit_from_remark()` 和 `_extract_unit()` 方法扩展**

#### 新增识别模式（按优先级排序）

| 优先级 | 模式 | 示例 | 提取结果 |
|--------|------|------|---------|
| 1 | 明确标注 | `单位为ms` | `ms` |
| 2 | LSB 格式 | `LSB=1ms` | `1ms` |
| 3 | 括号内 | `(ms)` 或 `[Hz]` | `ms` / `Hz` |
| 4 | 组合单位 | `km/h`、`°/h`、`m/s` | 完整组合单位 |
| 5 | 行尾单位 | `...时间处理ms` | `ms` |
| 6 | 常见单位 | `Hz`、`V`、`A` | 对应单位 |

#### 特殊处理
- **LSB 格式**：`LSB=1ms` 中能提取出 `1ms`（包括数字部分）
- **组合单位优先级**：`km/h` 和 `°/h` 的匹配优先于单个字符（如 `h`）
- **负向预检查**：避免误识别如 `×21` 中的 `21` 为单位

#### 支持的单位
- 时间：`ms`、`μs`、`s`、`min`、`h`
- 频率：`Hz`、`kHz`、`MHz`、`GHz`
- 角度/速率：`°`、`°/h`、`°/min`、`°/s`
- 速度：`m/s`、`m/s²`、`km/h`
- 温度：`℃`、`°C`
- 电气：`V`、`mV`、`A`、`mA`、`W`、`mW`、`dB`、`dBm`
- 数据：`bit`、`byte`、`KB`、`MB`
- 其他：`%`、`Ω`

### 4. 向后兼容性

所有改进完全保持向后兼容：
- 原有的 `乘以10` 格式继续工作，生成 `10X+0`
- 原有的单位提取逻辑保持不变
- 新格式作为额外选项，不影响现有数据处理

## 测试覆盖

### 公式标准化测试
- ✓ 基础格式：`乘以10` → `10X+0`
- ✓ 带偏移量：`乘以10+3` → `10X+3`
- ✓ 带负偏移：`乘以10-3` → `10X-3`
- ✓ 除法变体：`除以100±N` → `0.01X±N`
- ✓ 符号形式：`×10±N`、`÷100±N`
- ✓ 量化/分辨率：`量化单位N±M` → `NX±M`

### 单位提取测试
- ✓ LSB 格式：`LSB=1ms` → `1ms`
- ✓ 明确标注：`单位为ms` → `ms`
- ✓ 组合单位：`km/h` → `km/h`（优先级优于 `h`）
- ✓ 括号内：`(ms)` / `[Hz]` → 对应单位
- ✓ 防误识别：`×21` 不被识别为单位

### 公式检测测试
- ✓ 识别新格式：`乘以10+3` 被识别为公式
- ✓ 排除误识别：`212×21` 不被识别为公式
- ✓ 保持现有准确性：所有历史测试用例通过

## 使用建议

### 对于数据维护者
- 推荐使用新格式处理更复杂的转换场景
- 例如：`乘以10+273`（从摄氏度转凯尔文）
- 例如：`除以100+10`（带初值偏移的缩放）

### 对于数据输入
- 单位字段仍然是首选，标红提取的单位只作为备选
- 对于复杂的公式或单位组合，建议在数据处理方法或备注中明确标注
- 使用括号或明确的标签（如"单位为"）可以提高识别准确率

## 性能影响

- 正则表达式复杂度有所增加，但性能影响微乎其微（< 1ms 每行）
- 单位提取由于增加了更多的模式，可能多调用 1-2 次正则匹配，但总体仍在可接受范围内

## 后续改进空间

1. 可考虑添加更多数学操作符（如 `乘以N倍` 等表述）
2. 可支持更多复杂的单位组合（如 `kg·m/s²`）
3. 可引入机器学习模型改进单位识别准确率
