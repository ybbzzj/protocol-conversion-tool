# 协议转换工具 - 使用说明及交付指南

本工具是一款专为协议文档设计的 Excel 自动提取与匹配系统，集成了 ERNIE 3.0 语义匹配算法，支持离线运行。

## 🚀 启动步骤

1.  **准备环境**：确保电脑已安装 Python 3.8 或更高版本。
2.  **一键启动**：双击根目录下的 `启动工具.bat`。
    *   脚本会自动检查并安装必要的依赖（如 `Flask`, `PaddleNLP`, `Openpyxl` 等）。
    *   脚本会自动清理残留进程，确保 5001 端口可用。
3.  **访问系统**：在浏览器地址栏输入：`http://localhost:5001`。

---

## 📦 交付文件清单

如果您需要将此工具打包，**必须包含**以下文件和文件夹：

### 核心程序
*   `backend/`：后端逻辑、路由及配置文件。
*   `public/dist/`：前端编译产物（这是网页显示的全部内容）。
*   `启动工具.bat`：客户唯一的入口文件。

### 资源与模型
*   `models/ernie-3.0-nano-zh/`：**关键！** 这是离线语义匹配模型，必须包含，否则客户无法离线使用语义匹配。
*   `word/csvfile/协议模板.xlsx`：导出 Excel 的标准模板。

### 配置文件（可选，建议保留）
*   `backend/config_protocol_fields.json`：预定义的协议字段配置。
*   `backend/data/knowledge_base.json`：历史匹配知识库。

> **注意**：不需要交付 `public/src/`（源代码）、`.git/` 或 `node_modules/`。

---

## 💡 常见问题及解决方法

### 1. 访问页面显示 "Not Found" 或 "Frontend dist not found"
*   **原因**：`public/dist` 文件夹不存在或其中的 `index.html` 丢失。
*   **解决**：确保交付包中包含完整的 `public/dist` 目录。如果在开发环境下，请先在 `public` 目录运行 `npm run build`。

### 2. 下载生成的 Excel 时变成下载 HTML 文件
*   **原因**：后端路由冲突或未正确识别下载接口。
*   **解决**：目前已在 `app.py` 中增加了自动重定向和手动托管逻辑，请确保使用的是最新版本的 `backend/app.py`。

### 3. 语义匹配没有效果或报错
*   **原因**：缺少 `models/` 下的模型文件，或者电脑没有联网且没有离线模型。
*   **解决**：检查 `models/ernie-3.0-nano-zh/` 目录下是否有 `model_state.pdparams` 等文件。

### 4. 端口被占用
*   **解决**：`启动工具.bat` 会自动尝试关闭占用 5001 端口的旧进程。如果仍然失败，请手动关闭所有命令行窗口，或在任务管理器中结束 `python.exe` 进程。

---

## ⚠️ 注意事项

1.  **浏览器建议**：请使用 Chrome、Edge 或火狐浏览器，避免使用低版本 IE。
2.  **路径不要含中文**：建议将项目放在纯英文路径下（如 `D:\Tools\protocol-tool`），以避免部分 Python 库在处理路径时出现编码问题。
3.  **Excel 占用**：在生成或下载 Excel 结果时，请确保本地没有打开同名的输出文件，否则可能导致写入失败。
