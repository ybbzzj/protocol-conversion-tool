# 智能预映射工作流程图

## 完整流程时序图

```mermaid
sequenceDiagram
    participant User as 用户
    participant FE as 前端系统
    participant BE as 后端系统
    participant DB as 配置文件
    participant FS as 文件系统

    User->>FE: 1. 进入协议提取页面
    FE->>BE: 2. 请求字段配置
    BE->>DB: 3. 读取协议字段配置
    DB-->>BE: 4. 返回20个可用字段
    BE-->>FE: 5. 返回字段列表
    FE-->>User: 6. 显示字段选择界面
    
    User->>FE: 7. 选择字段[参数,数据类型,单位,备注,值域]
    User->>FE: 8. 上传测试文档
    FE->>BE: 9. 启动提取任务(含字段ID)
    BE->>DB: 10. 解析字段ID为字段名称
    DB-->>BE: 11. 返回期望字段名称
    BE->>FS: 12. 保存任务和期望字段
    BE-->>FE: 13. 返回任务ID和期望字段
    FE-->>User: 14. 显示任务启动成功
    
    loop 文档处理过程
        FE->>BE: 15. 查询任务状态
        BE->>FS: 16. 处理文档并提取字段
        BE->>BE: 17. 计算映射质量评分(0.583)
        FS-->>BE: 18. 返回处理结果
        BE-->>FE: 19. 返回进度和质量评分
        FE-->>User: 20. 实时显示处理进度
    end
    
    User->>FE: 21. 任务完成，查看结果
    FE->>BE: 22. 获取最终状态
    BE->>BE: 23. 智能分流决策(评分0.583→强制修正)
    BE-->>FE: 24. 返回分流策略
    alt 质量评分 > 0.9
        FE-->>User: 25A. 直接下载文件
    else 质量评分 > 0.7
        FE-->>User: 25B. 可选字段修正
    else 质量评分 ≤ 0.7
        FE-->>User: 25C. 强制字段修正
        User->>FE: 26. 进入字段映射页面
        FE->>BE: 27. 获取映射预览
        BE-->>FE: 28. 返回映射建议
        FE-->>User: 29. 显示映射修正界面
    end
```

## 状态流转图

```mermaid
stateDiagram-v2
    [*] --> 字段选择
    字段选择 --> 任务启动
    任务启动 --> 文档处理
    文档处理 --> 质量评估
    质量评估 --> 智能分流
    
    智能分流 --> 直接下载: 评分 > 0.9
    智能分流 --> 可选修正: 评分 > 0.7
    智能分流 --> 强制修正: 评分 ≤ 0.7
    
    直接下载 --> [*]
    可选修正 --> 字段映射
    强制修正 --> 字段映射
    字段映射 --> [*]
```

## 数据流转图

```mermaid
graph TD
    A[用户选择字段ID] --> B{前端处理}
    B --> C[字段ID数组: [1,2,3,4,5]]
    C --> D[POST /api/extract/start]
    D --> E{后端处理}
    E --> F[解析字段ID为名称]
    F --> G[期望字段: [参数,数据类型,单位,备注,值域]]
    G --> H[保存任务状态]
    H --> I[返回任务ID和期望字段]
    
    I --> J{文档处理}
    J --> K[提取实际字段: 6个]
    K --> L[质量评分计算: 0.583]
    L --> M[精确匹配: 0, 模糊匹配: 5, 未匹配: 1]
    M --> N[智能分流决策]
    N --> O{分流策略}
    
    O --> P[直接下载: 评分>0.9]
    O --> Q[可选修正: 评分>0.7] 
    O --> R[强制修正: 评分≤0.7]
    
    R --> S[GET /api/mapping/preview/{task_id}]
    S --> T[返回映射建议]
    T --> U[前端显示修正界面]
```

## 核心组件交互图

```mermaid
graph LR
    A[前端Vue组件] --> B[Extract.vue]
    B --> C[字段选择组件]
    B --> D[任务管理组件]
    B --> E[状态显示组件]
    
    F[后端Flask API] --> G[extract.py]
    G --> H[field_matcher.py]
    G --> I[mapping.py]
    G --> J[config.py]
    
    K[配置文件] --> L[config_protocol_fields.json]
    L --> M[字段定义和ID映射]
    
    N[处理引擎] --> O[TableDetector]
    O --> P[DataCleaner]
    P --> Q[ExcelExporter]
    
    R[质量评估] --> S[EnhancedFieldMatcher]
    S --> T[匹配算法]
    T --> U[评分计算]
    
    V[智能分流] --> W[质量阈值判断]
    W --> X[策略决策]
    X --> Y[前端路由控制]
```

## 测试验证覆盖图

```mermaid
graph TB
    A[完整流程测试] --> B[字段传递测试]
    A --> C[质量评估测试]
    A --> D[智能分流测试]
    A --> E[接口调用测试]
    A --> F[异常处理测试]
    
    B --> B1[字段ID解析]
    B --> B2[期望字段传递]
    B --> B3[配置文件兼容性]
    
    C --> C1[评分算法验证]
    C --> C2[匹配类型统计]
    C --> C3[评分等级判断]
    
    D --> D1[高分策略]
    D --> D2[中分策略]
    D --> D3[低分策略]
    
    E --> E1[API响应时间]
    E --> E2[数据格式验证]
    E --> E3[状态码检查]
    
    F --> F1[空参数处理]
    F --> F2[超时处理]
    F --> F3[错误恢复]
```

## 关键时间点记录

| 时间戳 | 步骤 | 动作 | 状态 | 备注 |
|--------|------|------|------|------|
| 14:47:04 | 步骤1 | 获取字段配置 | 完成 | 20个可用字段 |
| 14:47:04 | 步骤2 | 用户选择字段 | 完成 | 选择前5个字段 |
| 14:47:04 | 步骤3 | 启动提取任务 | 完成 | 任务ID生成 |
| 14:47:04 | 步骤4 | 期望字段传递 | 验证通过 | 正确传递5个字段 |
| 14:47:04 | 步骤5 | 文档处理开始 | 处理中 | 进度0% |
| 14:47:04 | 步骤6 | 质量评分计算 | 完成 | 评分0.583 |
| 14:47:04 | 步骤7 | 智能分流决策 | 完成 | 强制修正策略 |
| 14:47:04 | 步骤8 | 映射预览获取 | 完成 | 接口200 OK |

## 性能监控指标

```mermaid
graph LR
    A[响应时间] --> B[< 100ms: 字段配置]
    A --> C[< 200ms: 任务启动]
    A --> D[< 3s: 文档处理]
    A --> E[< 100ms: 状态查询]
    
    F[资源使用] --> G[内存 < 50MB]
    F --> H[CPU < 10%]
    F --> I[网络 6次请求]
    F --> J[文件IO 1次]
    
    K[成功率] --> L[字段传递: 100%]
    K --> M[质量评估: 100%]
    K --> N[分流决策: 100%]
    K --> O[接口调用: 100%]
```

这个完整的测试文档和流程图展示了智能预映射功能从用户操作到系统响应的完整生命周期，包括时序、状态、数据流转和性能监控等各个维度的详细信息。